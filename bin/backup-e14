#!/bin/bash

cd ~/e14-backup

# To get this working on macOS you may need to give "Full Disk Access" to /usr/sbin/cron in System Preferences

ssh e14@siyeah.com 'bzip2 -c ~/e14/db.sqlite3 > db.sqlite3.bz2'
scp -C e14@siyeah.com:~/db.sqlite3.bz2 .
mv db.sqlite3.bz2 db.sqlite3-`date +%s`.bz2
